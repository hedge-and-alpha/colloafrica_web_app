import { ComposeGradientPipe } from './compose-gradient.pipe';

describe('ComposeGradientPipe', () => {
  it('create an instance', () => {
    const pipe = new ComposeGradientPipe();
    expect(pipe).toBeTruthy();
  });
});
