import { ButtonPurpleDirective } from './button-purple.directive';

describe('ButtonPurpleDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonPurpleDirective();
    expect(directive).toBeTruthy();
  });
});
