import { ButtonDangerDirective } from './button-danger.directive';

describe('ButtonDangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDangerDirective();
    expect(directive).toBeTruthy();
  });
});
