import { ButtonSecondaryDirective } from './button-secondary.directive';

describe('ButtonSecondaryDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSecondaryDirective();
    expect(directive).toBeTruthy();
  });
});
