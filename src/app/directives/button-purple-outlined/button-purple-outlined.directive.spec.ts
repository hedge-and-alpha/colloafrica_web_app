import { ButtonPurpleOutlinedDirective } from './button-purple-outlined.directive';

describe('ButtonPurpleOutlinedDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonPurpleOutlinedDirective();
    expect(directive).toBeTruthy();
  });
});
