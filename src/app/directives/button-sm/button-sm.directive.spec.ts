import { ButtonSmDirective } from './button-sm.directive';

describe('ButtonSmDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSmDirective();
    expect(directive).toBeTruthy();
  });
});
