import { ButtonSuccessDirective } from './button-success.directive';

describe('ButtonSuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSuccessDirective();
    expect(directive).toBeTruthy();
  });
});
